texto = document.getElementById('texto');

// Eventos: janela:
    // onfocus: dispara quando a janela recebe o foco do cursor do mouse ou do teclado.

    // onblur: dispara quando a janela perde o foco do cursor do mouse ou do teclado.

    // onchange: quando o usuário altera o valor de um campo de formulário e sai do campo (perde o foco). Ele é muito utilizado com o elemento select, mas também pode ser usado com input e textarea


function acao() {
    console.log("Evento disparado");
}