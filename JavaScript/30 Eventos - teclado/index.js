texto = document.getElementById('texto');

// Eventos: teclado:
    // onkeydown: quando o usuário pressiona uma tecla. No caso da exibição de caracteres, o evento é disparado antes do caractere ser exibido na tela.

    // onkeyup: quando o usuário solta uma tecla.

    // onkeypress: enquanto o usuário mantém a tecla pressionada. Só funciona para teclas que geram caracteres (letras, números, símbolos).


function acao() {
    alert("Evento disparado");
}